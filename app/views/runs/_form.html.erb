<%
if !@run.user.nil?
  Time.zone = @run.user.time_zone
else
  Time.zone = 'Brasilia'
end
%> 
<%= form_for(@run, :html => {:class => 'form-horizontal'}) do |f| %>
  <% if @run.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@run.errors.count, "error") %> prohibited this run from being saved:</h2>

      <ul>
      <% @run.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="from-group">
    <%= f.label :user_id %><br>
    <%= collection_select :run, :user_id, User.order('name ASC'), :id, :name, {:selected => @run.user_id, prompt: 'Select Runner'}, :class => 'form-control'%>
  </div>

  <div class="form-group">
    <%= f.label :datetime %><br>
    <%= datetime_select :run, :datetime %>
  </div>
  <div class="field">
    <%= f.label :distance, :class => 'form-control' %><br>
    <%= f.text_field :distance, :class => 'form-control' %>
  </div>
  <div class="field">
    <%= f.label :duration %><br>
    <%= f.text_field :duration_formated, label: 'Inform duration: ', placeholder: 'HH:MM:SS', :data=> {:mask=> '23:59:59'}%>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note, :class => 'form-group'%>
  </div>  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
